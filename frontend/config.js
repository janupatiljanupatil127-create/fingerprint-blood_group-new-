// Set this to your deployed Render backend URL once you have it,
// e.g. "https://fingerprint-blood-prediction-api.onrender.com"
// Leave as localhost for local testing against `python app.py`.
const API_BASE_URL = "http://localhost:5000";
